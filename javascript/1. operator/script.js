// tipe data : number, string, boolean, object, function, undefined
// jika string dibagi integer hasilnya NaN
// NaN yaitu Not a Number.

// OPERATOR 
// operator aritmatika digunakan untuk perhitungan : +, -, *, /, %.

// operator penugasan digunakan untuk string variabel : =, -=, +=, *=, /=, %=.

// operator perbandingan digunakan untuk membandingkan antar nilai menghasilkan boolean true/false :
// == (sama dengan), != (tidak sama dengan), ===(strict sama dengan), !==(strict tidak sama dengan), > (lebih besar dari), < (lebih kecil dari), >= (lebih besar sama dengan), <= (lebih kecik sama denga dengan).
// strict yaitu membandingkan antar tipe data misal integer dibandingkan dengan string maka hasilnya false.

// operator logika = && (dan), || (atau), ! (bukan).
// jika menggunakan && harus memiliki 2 perbandingan dan keduanya harus bernilai true untuk menghasilkan true
// jika menggunakan || harus memiliki 2 perbandingan dan salah satunya boleh true/false untuk menghasilkan true.
// jika menggunakan ! maka akan membalikan nilai true menjadi false dan sebaliknya

// operator string digunakan untuk menambahkan string dan semua tipe data yg dicampur strring makan hasilnya string : +.
// jika integer + string maka hasilnya akan string walaupun integer itu angka.

// operator typeof() diugunakan untuk mengidentifikasi apakah data nilainya string/integer/boolean.
// typeof (masukan data yg ingin diidentifikasi).

// operator penkondisian digunakan untuk menentukan 2 perbandingan dan menampilkan true/false. 
// (kondisi) ? true : false.
// true/false bisa berupa string / integer / function.

